# 2026-08-01 Memory

## 数据管理设置 UI 全面重构

### 完成事项

1. **备份与恢复方案重新设计**
   - 完整备份：打包 database.db + settings.json + subscriptions.opml 为单个 ZIP 文件
   - 恢复粒度：全部恢复、仅恢复配置、仅恢复订阅、仅恢复数据库（四个选项）
   - 跨设备导入：支持上传 ZIP 备份文件，验证后选择恢复粒度

2. **UI 布局重新组织**
   - 完整备份与恢复（含导入备份按钮 + 备份列表 + 四个恢复操作按钮）
   - 配置导入导出（导入/导出配置 JSON）
   - 数据库维护（导入/导出数据库、压缩、重启应用）
   - 缓存清理
   - 备份策略（保留数量/天数、自动备份开关、间隔）
   - 文章留存策略
   - 统计信息

3. **自定义弹窗替代浏览器 confirm**
   - 导入备份后弹出内部 modal 选择恢复粒度
   - 恢复单个备份时也使用内部 modal

4. **Wails WindowState 持久化修复**
   - 原来使用 `WindowExecJS`（void 函数）做 localStorage 读写，改为文件读写（`os.ReadFile`/`os.WriteFile`）

5. **后端 API 新增**
   - `POST /backups/restore/import`：上传 ZIP 并返回内容结构（含DB/配置/订阅）
   - `POST /backups/{name}/restore`：全量恢复
   - `POST /backups/{name}/restore-config`：仅恢复配置
   - `POST /backups/{name}/restore-opml`：仅恢复订阅
   - `POST /backups/{name}/restore-db`：仅恢复数据库

### 修改文件
- `apps/web/src/components/settings/SettingsDataTab.tsx` — 完全重写
- `server/go/internal/handlers/reader.go` — 新增 ImportBackupAndValidate handler
- `server/go/internal/services/backup.go` — 新增 GetBackupContentsFromPath 方法
- `apps/desktop/app.go` — 修复 WindowExecJS 错误，改用文件持久化

## 2026-08-01 后续修复

### 问题
- 备份列表不显示（TypeScript 编译错误）
- 弹窗按钮无法点击（onclick 作用域问题）
- 缺少 doClearReadability/doRebuildSearch/doVacuum 等函数定义
- 使用已删除的 SectionCacheCleanup/SectionStatistics 等组件

### 修复方案
- 完全重写 SettingsDataTab.tsx，移除所有未使用/不存在的函数和组件
- 移除"配置导入导出"、"数据库维护"、"缓存清理"、"统计信息"四个区块
- 修复弹窗按钮点击问题：改用 document.createElement + onclick 直接赋值，不使用 innerHTML onclick
- 移除 restore-db（后端无此端点）
- TypeScript 零错误，前端构建成功，Go 后端编译通过

